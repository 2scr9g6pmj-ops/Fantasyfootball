services:
  - type: web
    name: fantasyfootball-lineup-assistant
    runtime: python
    plan: starter
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
    disk:
      name: fantasyfootball-data
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.7
      - key: DATABASE_URL
        value: sqlite:////var/data/fantasyfootball.db
      - key: SLEEPER_USERNAME
        value: CousinsFF
      - key: YAHOO_CLIENT_ID
        sync: false
      - key: YAHOO_CLIENT_SECRET
        sync: false
      - key: APP_ENCRYPTION_KEY
        generateValue: true
      - key: APP_ACCESS_USERNAME
        value: Alexa
      - key: APP_ACCESS_PASSWORD
        sync: false
